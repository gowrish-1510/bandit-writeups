Question-The password for the next level is stored in the file data.txt next to the word millionth.

commands used- ls , grep

1.ls
We discover a file called data.txt in the directory.

2.grep
Since the password is present next to a word "millionth" in the file, we use grep -w "millionth" data.txt  command to find the password in the text. grep is a command used for finding patterns in a file.

password- TESKZC0XvTetK0S9xNwm25STk5iWrBvP

info:- grep -c "word_to_be_counted" file.txt returns count value of that word in the file

EN632PlfYiZbn3PhVK3XOGSlNInNE00t
