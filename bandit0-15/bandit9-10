Question- The password for the next level is stored in the file data.txt in one of the few human-readable strings, preceded by several ‘=’ characters.

commands used- ls ,cat , strings, grep

1.ls

2.cat
When we do cat data.txt , we get random patterns on the screen which are not human readable.

3.piping strings and grep command
The string command displays the human readable strings in the file and the grep command will be used to find the occurance of '=' characters. Hence, piping:-
 strings data.txt | grep "===" 
 
 password:- G7w8LIi6J3kTb8A7j9LgrywtEUlyyp6s 
